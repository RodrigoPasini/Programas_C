#include<stdio.h>
#include<stdlib.h>

int main()
{
    int distancia, litros = 36, autonomia = 12;
    distancia = litros*autonomia;
    printf ("A distancia maxima percorrida pelo veiculo serah de: km %.i\n", distancia);
    system ("pause");
    return (0);

}
